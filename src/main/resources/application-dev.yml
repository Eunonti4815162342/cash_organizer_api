spring:
  datasource:
    url: jdbc:postgresql://192.168.1.145:5432/llama_finanzas
    username: llama_user
    password: llama_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  liquibase:
    enabled: false
    change-log: classpath:db/changelog/db.changelog-master.xml
    default-schema: llama_finanzas

application:
  security:
    jwt:
      secret-key: dev_secret_key_must_be_changed_in_production_404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 86400000
    cors:
      allowed-origins: http://localhost:8080,http://localhost:8085,http://localhost:3000,http://127.0.0.1:3000,http://192.168.1.145:8086,http://192.168.1.145:8085

server:
  port: 8085
  error:
    include-message: always
    include-stacktrace: always

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: WARN
    org.springframework: WARN
    org.springframework.boot: WARN
    org.springframework.security: WARN
    com.tritit.cashorganizer.api: INFO
    org.hibernate: WARN
    org.liquibase: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/cash-organizer-dev.log
